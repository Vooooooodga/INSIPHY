"""Compatibility exports. Implementations live in the documented submodules."""


# Backward-compatible symbol exports; no alternate implementations.
from insiphy.verification.benchmark import (
    NON_BIOLOGICAL_BENCHMARK_CLASSES,
    COPY_CONTEXT_CLASSES,
    AMBIGUOUS_CLASSES,
    event_key,
    call_scope,
    benchmark_events,
)
